programa {
	funcao inicio() {
		///Desenvolver um sistema que efetue a soma de todos os números ímpares que são múltiplos de três e que se encontram no conjunto dos números de 1 até 500
	inteiro soma = 0
	para (inteiro numero = 1; numero <= 500; numero ++ ){
	    se (numero % 2 != 0)
	        se (numero % 3 == 0)
	            soma = soma + numero
	             
	            
	        }
	         escreva("A soma dos numero impares multiplos de tres é de: ", soma)
	    }
	   
	}
	

     
